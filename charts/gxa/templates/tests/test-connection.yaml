apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gxa.fullname" . }}-test-connection"
  labels:
    {{- include "gxa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gxa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
