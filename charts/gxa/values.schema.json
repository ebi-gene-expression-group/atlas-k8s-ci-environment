{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "expIds": {
      "type": "string",
      "description": "Space-separated list of experiment IDs to populate from the ftp server. Each ID must start with E-.",
      "pattern": "^(E-[^\\s]+\\s?)+$"
    },
    "runPopulatorJob": {
      "type": "boolean",
      "description": "Enable the populator job (set to true to run the job)",
      "default": false
    }
  },
  "additionalProperties": true
} 